import React from "react";

// creates the cards for the books to be displayed on, displays the icon title and children, which is taken in from the database
function Card({ icon, title, children }) {
  return (
    <div className="card mt-4">
      <div className="card-header">
        <h3>
          <strong>
            <i className={`fa fa-${icon}`} aria-hidden="true" /> {title}
          </strong>
        </h3>
      </div>
      <div className="card-body">{children}</div>
    </div>
  );
}

// exports the card to be reused
export default Card;
